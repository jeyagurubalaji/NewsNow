# Base URL the frontend calls for API requests (used directly in production builds)
VITE_API_BASE_URL=/api

# Target the Vite dev server proxies /api requests to (dev only, see vite.config.js)
VITE_API_PROXY_TARGET=http://localhost:8080
